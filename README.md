# @awesomeem/plugin-ai-discovery

**Status: stub (work-in-progress).** Published as a placeholder so the final name is reserved. No functional routes yet.

Planned:

- `/llms.txt` — structured content index for LLM crawlers
- `/ai-manifest.json` — machine-readable capabilities manifest
- JSON-LD injection into `<head>` for rich discovery

Implementation pending validation against the live EmDash plugin API (upstream is still at v0.1.0 developer preview). Do not depend on this package in production until 0.2.0.
