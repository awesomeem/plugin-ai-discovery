// TODO: implement llms.txt, ai-manifest, JSON-LD routes
// Placeholder — will be built when plugin API is tested against live EmDash

import { definePlugin } from "emdash";

export default definePlugin({
  routes: {},
});
